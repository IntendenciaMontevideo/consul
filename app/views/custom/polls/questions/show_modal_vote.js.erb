<% token = poll_voter_token(@poll, current_user) %>
$("#vote-modal-content").html('<%= j render("/polls/questions/vote_modal_info", session_answers: @session_answers, token: token, poll: @poll, is_create_session: false) %>');
App.Polls.replaceToken();
jQuery('#vote-modal').foundation('open');
