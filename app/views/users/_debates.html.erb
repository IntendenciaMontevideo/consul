<table class="margin-top">
  <thead>
    <th scope="col"><%= t("users.show.debates") %></th>
  </thead>
  <tbody>
    <% @debates.each do |debate| %>
      <tr id="debate_<%= debate.id %>">
        <td class="debates-profile">
          <%= link_to debate.title, debate %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @debates %>
